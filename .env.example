# Copy to .env and fill in. Never commit real keys.
OPENAI_API_KEY=sk-...

# Target system under test. Point at your own Grounded RAG app or any LLM endpoint.
TARGET_URL=http://localhost:8000/ask
# For a raw-model target instead of an app, set the model string:
# TARGET_MODEL=openai/gpt-4o-mini
